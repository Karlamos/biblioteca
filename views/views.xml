<odoo>
  <data>
    <!-- LIBRO -->
    <record model="ir.ui.view" id="biblioteca_libro_list">
      <field name="name">biblioteca libro list</field>
      <field name="model">biblioteca.libro</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="autor"/>
          <field name="a_pub"/>
        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_libro_form">
        <field name="name">biblioteca.libro.form</field>
        <field name="model">biblioteca.libro</field>
        <field name="arch" type="xml">
            <form string="Libro">
                <sheet>
                    <group>
                        <group>
                            <field name="isbn"/>
                            <field name="name"/>
                            <field name="autor"/>
                            <field name="a_pub"/>
                        </group>
                        <group>
                            <field name="fuente_datos" readonly="1"/>
                            <field name="portada_url" invisible="1"/>
                            <field name="portada_html" widget="html" readonly="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Descripción">
                            <field name="descripcion" placeholder="Escribe una descripción..."/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- AUTOR -->
    <record model="ir.ui.view" id="biblioteca_autor_list">
      <field name="name">biblioteca autor list</field>
      <field name="model">biblioteca.autor</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="autor"/>
          <field name="descripcion"/>
        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_autor_form">
      <field name="name">biblioteca autor form</field>
      <field name="model">biblioteca.autor</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group>
              <field name="autor"/>
              <field name="descripcion"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- PRESTAMO -->
    <record model="ir.ui.view" id="biblioteca_prestamo_list">
      <field name="name">biblioteca prestamo list</field>
      <field name="model">biblioteca.prestamo</field>
      <field name="arch" type="xml">
        <list string="Préstamos">
            <field name="name"/>
            <field name="fecha_prestamo"/>
            <field name="libro_id"/>        
            <field name="multa"/>
            <field name="estado"/>

        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_prestamo_form">
      <field name="name">biblioteca prestamo form</field>
      <field name="model">biblioteca.prestamo</field>
      <field name="arch" type="xml">
        <form string="Préstamo">
          <header>
            <button string ="Prestar" type="object" name="generar_prestamo"
              invisible="estado in ('p', 'd', 'm')" />
            <button name="action_imprimir_comprobante"
                    string="Ver Comprobante"
                    type="object"
                    class="btn-primary"/>


            <field name="estado" widget="statusbar"/>
          </header>
          <sheet>
            <group>
              <label for="name" string="Código de prestamo" />
              <field name="name" readonly = "estado in ('p', 'd', 'm')" nolabel="1"/>
              <field name="fecha_prestamo" readonly = "estado in ('p', 'd', 'm')"/>
              <field name="fecha_devolucion" readonly= "estado in ('p', 'd', 'm')" required="estado in ('p', 'm')"/>
              <field name="libro_id" readonly= "estado in ('p', 'd', 'm')" />
              <field name="usuario_id" readonly= "estado in ('p', 'd', 'm')"/>
              <field name="multa_bol" readonly= "estado in ('p', 'd', 'm')"/>
              <field name="multa" readonly= "estado in ('p', 'd', 'm')" invisible="multa_bol == False"/>
              <field name="fecha_maxima" readonly= "estado in ('p', 'd', 'm')"/>
              <field name="usuario" readonly= "estado in ('p', 'd', 'm')"/>
              <field name="multas_ids">  
                <list editable="bottom">
                  <field name="name_multa"/>
                  <field name="danado" readonly="perdida == True"/>
                  <field name="perdida" readonly="danado == True"/>
                  <field name="estado"/>
                </list>
              </field>  
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- MULTA -->
    <record model="ir.ui.view" id="biblioteca_multa_list">
      <field name="name">biblioteca multa list</field>
      <field name="model">biblioteca.multa</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="name_multa"/>
          <field name="prestamo"/>
          <field name="danado"/>
          <field name="perdida"/>
          <field name="costo_multa"/>
          <field name="estado"/>
        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_multa_form">
      <field name="name">biblioteca multa form</field>
      <field name="model">biblioteca.multa</field>
      <field name="arch" type="xml">
        <form>
          <sheet>
            <group>
              <field name="name_multa"/>
              <field name="prestamo"/>
              <field name="danado"/>
              <field name="perdida"/>
              <field name="costo_multa"/>
              <field name="estado"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- PERSONAL -->
    <record model="ir.ui.view" id="biblioteca_personal_list">
      <field name="name">biblioteca personal list</field>
      <field name="model">biblioteca.personal</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="codigo_empleado"/>
          <field name="nombre"/>
          <field name="cargo"/>
          <field name="emailper"/>
          <field name="turno"/> 
        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_personal_form">
      <field name="name">biblioteca personal form</field>
      <field name="model">biblioteca.personal</field>
      <field name="arch" type="xml">
        <form>
          <group>
            <field name="codigo_empleado" required="1"/>
            <field name="nombre"/>
            <field name="cargo"/>
            <field name="emailper"/>
            <field name="turno"/>
          </group>
        </form>
      </field>
    </record>

    <!-- USUARIO -->
    <record model="ir.ui.view" id="biblioteca_usuario_list">
      <field name="name">biblioteca usuario list</field>
      <field name="model">biblioteca.usuario</field>
      <field name="type">list</field>
      <field name="arch" type="xml">
        <list>
          <field name="nombre"/>
          <field name="email"/>
          <field name="direccion"/>
          <field name="libros_prestados"/>
          <field name="cedula"/>
          <field name="telefono"/>
        </list>
      </field>
    </record>

    <record model="ir.ui.view" id="biblioteca_usuario_form">
      <field name="name">biblioteca usuario form</field>
      <field name="model">biblioteca.usuario</field>
      <field name="arch" type="xml">
        <form>
          <group>
            <field name="nombre"/>
            <field name="email"/>
            <field name="direccion"/>
            <field name="libros_prestados"/>
            <field name="cedula"/>
            <field name="telefono"/>
          </group>
        </form>
      </field>
    </record>



    <!-- ACCIONES -->
    <record model="ir.actions.act_window" id="biblioteca_libro_action_window">
      <field name="name">Libros</field>
      <field name="res_model">biblioteca.libro</field>
      <field name="view_mode">list,form</field>
    </record>

    <record model="ir.actions.act_window" id="biblioteca_autor_action_window">
      <field name="name">Autores</field>
      <field name="res_model">biblioteca.autor</field>
      <field name="view_mode">list,form</field>
    </record>

    <record model="ir.actions.act_window" id="biblioteca_prestamo_action_window">
      <field name="name">Préstamos</field>
      <field name="res_model">biblioteca.prestamo</field>
      <field name="view_mode">list,form</field>
    </record>

    <record model="ir.actions.act_window" id="biblioteca_multa_action_window">
      <field name="name">Multas</field>
      <field name="res_model">biblioteca.multa</field>
      <field name="view_mode">list,form</field>
    </record>

    <record model="ir.actions.act_window" id="biblioteca_personal_action_window">
      <field name="name">Personal</field>
      <field name="res_model">biblioteca.personal</field>
      <field name="view_mode">list,form</field>
    </record>

    <record model="ir.actions.act_window" id="biblioteca_usuario_action_window">
      <field name="name">Usuarios</field>
      <field name="res_model">biblioteca.usuario</field>
      <field name="view_mode">list,form</field>
    </record>

    <!-- MENÚS -->
    <menuitem name="Biblioteca" id="biblioteca_menu"/>

    <menuitem name="Libros" id="biblioteca_libro_menu" parent="biblioteca_menu" action="biblioteca_libro_action_window"/>
    <menuitem name="Autores" id="biblioteca_autor_menu" parent="biblioteca_menu" action="biblioteca_autor_action_window"/>
    <menuitem name="Préstamos" id="biblioteca_prestamo_menu" parent="biblioteca_menu" action="biblioteca_prestamo_action_window"/>
    <menuitem name="Multas" id="biblioteca_multa_menu" parent="biblioteca_menu" action="biblioteca_multa_action_window"/>
    <menuitem name="Personal" id="biblioteca_personal_menu" parent="biblioteca_menu" action="biblioteca_personal_action_window"/>
    <menuitem name="Usuarios" id="biblioteca_usuario_menu" parent="biblioteca_menu" action="biblioteca_usuario_action_window"/>

  </data>
</odoo>
